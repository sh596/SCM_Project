package com.example.dudeulimproject.view.proceed_inter_view.repository

import com.example.dudeulimproject.data.remote.MainService
import javax.inject.Inject

class ProceedInterViewRepositoryImpl @Inject constructor(private val service: MainService): ProceedInterViewRepository {
    override fun updateAudio() {
    }
}